numpy>=1.24
pyvisa>=1.13
matplotlib>=3.7
# A VISA backend is also required for real hardware. Either a vendor VISA
# (NI/Keysight) or the pure-python fallback:
#   pip install pyvisa-py
# pyvisa-py is enough for TCP socket resources; serial additionally needs
# pyserial.
